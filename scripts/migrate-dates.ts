// This script has been moved to run directly in the browser.
// Please open your browser console and run this code:

/*
import { api } from "./convex/_generated/api";

async function migrateDates() {
  try {
    console.log("Starting date migration...");
    const result = await api.tasks.migrateDates();
    console.log(`Migration completed successfully! Updated ${result.updatedCount} tasks.`);
  } catch (error) {
    console.error("Migration failed:", error);
  }
}

migrateDates();
*/
